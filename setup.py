from setuptools import setup, find_packages

setup(name='graddy',
      version='0.0.1',
      description='Graddy ML Framework',
      author='Shea Cardozo',
      author_email='sacardoz@uwaterloo.ca',
      packages=find_packages(),
      install_requires=[],
      zip_safe=False)
